# Optional — enables the AI "Take Photo" DO document scan (Google Gemini).
# Leave unset to hide that feature; manual DO entry still works.
# In GitHub Actions, add this as a repository secret named VITE_GEMINI_KEY.
VITE_GEMINI_KEY=
